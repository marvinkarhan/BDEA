services:
  db:
    image: mysql:8.0
    container_name: movie_tv_db
    environment:
      MYSQL_ROOT_PASSWORD: o6&Ufzbe6y6wrWjM
    volumes:
      - ./data:/docker-entrypoint-initdb.d
    ports:
      - "3306:3306"